{
  "name": "agent",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lite-server",
    "reset": "truffle migrate --reset",
    "start": "lite-server",
    "deploy:contract": "truffle deploy",
    "migrate": "truffle migrate",
    "css:watch": "npx tailwindcss -i ./src/css/tailwind/index.css -o ./src/css/dist/index.css --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "solc": "^0.4.24",
    "web3": "^0.20.0"
  },
  "devDependencies": {
    "lite-server": "^2.6.1",
    "tailwind": "^4.0.0",
    "truffle": "^5.6.4"
  }
}
